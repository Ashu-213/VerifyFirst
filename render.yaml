services:
  - type: web
    name: verifyfirst-api
    runtime: python
    buildCommand: pip install -r requirements-prod.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
