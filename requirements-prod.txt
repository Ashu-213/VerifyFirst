# VerifyFirst — Production Runtime Dependencies
# These are the minimal dependencies needed to RUN the server (not train)
# Use this for deployment to reduce build time and image size

fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
scikit-learn==1.4.2
numpy==1.26.4
python-whois==0.9.4
